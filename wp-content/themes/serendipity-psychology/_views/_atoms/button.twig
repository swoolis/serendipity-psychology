<a href="{{ link }}" class="btn btn-{{ colour }} {{ classes }}" {{ attributes }} {{ target ? 'target="' ~ target ~ '"' }} aria-label="button" {% if tracking %}onClick="{{ tracking }}"{% endif %}>{{ text }} {{ fn('icon','angles-right')}}</a>